<environment include="Development">
    <script type="text/javascript">
        $(document).ready(function(){
            $("#BioEditorForm").submit(function(e){
                e.preventDefault();
                
                /*
                var DeveloperId = $('#DeveloperId').val();
                var ShortBioCountry = $('#CurrentCity').val();
                var ShortBioAvailability = false;
                var ShortBioDescription = $('#ShortDescription').val();
                
                if ($('#IsAvailableForJob').is(":checked"))
                {
                  ShortBioAvailability = true;
                }
                alert(ShortBioDescription + DeveloperId + ShortBioCountry + ShortBioAvailability);                
                 */
                
                $.post('', $('#BioEditorForm').serialize(), function (response) {
                     alert('Posted using jQuery' + response);
                });
            });
        });
    </script>
</environment>