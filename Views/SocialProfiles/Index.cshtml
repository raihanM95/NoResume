@model IEnumerable<NoResume.Models.SocialProfile>

@{
    ViewData["Title"] = "Index";
    Layout = "_LayoutPrivate";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LinkedInUsername)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FacebookUsername)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TwitterUsername)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LinkedInUsername)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FacebookUsername)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TwitterUsername)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeveloperId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeveloperId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeveloperId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
